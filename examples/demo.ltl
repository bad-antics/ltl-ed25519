// examples/demo.ltl — runnable demo for ltl_ed25519

import std::io
use ltl_ed25519 as lib

fn main() {
    io::println("ltl_ed25519 demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}